export type LoginUserInput = {
    username: string;
    password: string;
}

export type CreateUserInput = {
    email: string;
    password: string;
    fullName: string;
    username: string;
}